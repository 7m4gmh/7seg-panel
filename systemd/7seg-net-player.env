# /etc/default/7seg-net-player のサンプル（このリポジトリ内のテンプレート）
# システム導入時は以下をコピー:
#   sudo install -m 644 systemd/7seg-net-player.env /etc/default/7seg-net-player

# バイナリの設置先ディレクトリ（本リポジトリ直下を想定）
WORKDIR=/home/radxa/7seg-panel

# 再生モード: flv または ts
MODE=flv

# 表示設定名（config.json 内の名前）
CONFIG=16x12

# 待受ポート
PORT=5004

# EOS/ERRORで即終了するか（サービス再起動と組み合わせる想定）
EXIT_ON_EOF=1

# 必要に応じて追加の環境変数をここに定義可能
# 例: GST_DEBUG=2